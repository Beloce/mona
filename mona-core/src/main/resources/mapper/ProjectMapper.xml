<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiangyang.dao.ProjectMapper">

    <resultMap id="BaseResultMap" type="com.xiangyang.model.ProjectDO">
        <id column="project_id" jdbcType="BIGINT" property="projectId" />
        <result column="gmt_create" jdbcType="TIMESTAMP" property="gmtCreate" />
        <result column="gmt_modified" jdbcType="TIMESTAMP" property="gmtModified" />
        <result column="father_project_id" jdbcType="VARCHAR" property="fatherProjectId" />
        <result column="project_level" jdbcType="INTEGER" property="projectLevel" />
        <result column="project_name" jdbcType="VARCHAR" property="projectName" />
        <result column="leader_user_id" jdbcType="BIGINT" property="leaderUserId" />
    </resultMap>

    <sql id="Query_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                <trim prefix="(" prefixOverrides="and" suffix=")">
                    <foreach collection="criteria.criteria" item="criterion">
                        <choose>
                            <when test="criterion.noValue">
                                and ${criterion.condition}
                            </when>
                            <when test="criterion.singleValue">
                                and ${criterion.condition} #{criterion.value}
                            </when>
                            <when test="criterion.betweenValue">
                                and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                            </when>
                            <when test="criterion.listValue">
                                and ${criterion.condition}
                            <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                #{listItem}
                            </foreach>
                            </when>
                        </choose>
                    </foreach>
                </trim>
                </if>
            </foreach>
        </where>
    </sql>

    <sql id="Update_By_Query_Where_Clause">
        <where>
            <foreach collection="query.oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>

    <sql id="Base_Column_List">
        project_id,gmt_create,gmt_modified,father_project_id,project_level,project_name,leader_user_id
    </sql>

    <select id="selectByQuery" parameterType="com.xiangyang.query.ProjectQuery" resultMap="BaseResultMap">
        select
        <if test="distinct">
            distinct
        </if>
        <include refid="Base_Column_List" />
            from mona_project
        <if test="_parameter != null">
            <include refid="Query_Where_Clause" />
        </if>
        <if test="orderByClause != null">
            order by ${orderByClause}
        </if>
        <include refid="MysqlDialectSuffix" />
    </select>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" /> from mona_project where project_id = #{projectId,jdbcType=BIGINT}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from mona_project where project_id = #{projectId,jdbcType=BIGINT}
    </delete>

    <delete id="deleteByQuery" parameterType="com.xiangyang.query.ProjectQuery">
        delete from mona_project
        <if test="_parameter != null">
            <include refid="Query_Where_Clause" />
        </if>
    </delete>

    <insert id="insertSelective" parameterType="com.xiangyang.model.ProjectDO">
        <selectKey keyProperty="projectId" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into mona_project
        <trim prefix="(" suffix=")" suffixOverrides=",">
            gmt_create,gmt_modified,
            <if test="fatherProjectId != null">
                father_project_id,
            </if>
            <if test="projectLevel != null">
                project_level,
            </if>
            <if test="projectName != null">
                project_name,
            </if>
            <if test="leaderUserId != null">
                leader_user_id,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            now(),now(),
            <if test="fatherProjectId != null">
                #{fatherProjectId,jdbcType=VARCHAR},
            </if>
            <if test="projectLevel != null">
                #{projectLevel,jdbcType=INTEGER},
            </if>
            <if test="projectName != null">
                #{projectName,jdbcType=VARCHAR},
            </if>
            <if test="leaderUserId != null">
                #{leaderUserId,jdbcType=BIGINT},
            </if>
        </trim>
    </insert>

    <select id="countByQuery" parameterType="com.xiangyang.query.ProjectQuery" resultType="java.lang.Integer">
        select count(*) from mona_project
        <if test="_parameter != null">
            <include refid="Query_Where_Clause" />
        </if>
    </select>

    <update id="updateByQuerySelective" parameterType="map">
        update mona_project
        <set>
            gmt_modified=now(),
            <if test="record.fatherProjectId != null">
                father_project_id = #{record.fatherProjectId,jdbcType=VARCHAR},
            </if>
            <if test="record.projectLevel != null">
                project_level = #{record.projectLevel,jdbcType=INTEGER},
            </if>
            <if test="record.projectName != null">
                project_name = #{record.projectName,jdbcType=VARCHAR},
            </if>
            <if test="record.leaderUserId != null">
                leader_user_id = #{record.leaderUserId,jdbcType=BIGINT},
            </if>
        </set>
        <if test="_parameter != null">
            <include refid="Update_By_Query_Where_Clause" />
        </if>
    </update>

    <update id="updateByQuery" parameterType="map">
        update mona_project
        father_project_id = #{record.fatherProjectId,jdbcType=VARCHAR},
        project_level = #{record.projectLevel,jdbcType=INTEGER},
        project_name = #{record.projectName,jdbcType=VARCHAR},
        leader_user_id = #{record.leaderUserId,jdbcType=BIGINT},
        gmt_modified = now()
        <if test="_parameter != null">
            <include refid="Update_By_Query_Where_Clause" />
        </if>
    </update>

    <update id="updateByPrimaryKeySelective" parameterType="com.xiangyang.model.ProjectDO">
        update mona_project
        <set>
            gmt_modified=now(),
            <if test="fatherProjectId != null">
                father_project_id = #{fatherProjectId,jdbcType=VARCHAR},
            </if>
            <if test="projectLevel != null">
                project_level = #{projectLevel,jdbcType=INTEGER},
            </if>
            <if test="projectName != null">
                project_name = #{projectName,jdbcType=VARCHAR},
            </if>
            <if test="leaderUserId != null">
                leader_user_id = #{leaderUserId,jdbcType=BIGINT},
            </if>
        </set>
        where project_id = #{projectId,jdbcType=BIGINT}
    </update>

    <sql id="MysqlDialectSuffix">
        <if test="pageOffset &gt;= 0 and pageSize &gt; 0">
            <![CDATA[ LIMIT #{pageOffset,jdbcType=INTEGER}, #{pageSize,jdbcType=INTEGER} ]]>
        </if>
    </sql>

</mapper>
